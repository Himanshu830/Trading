import React from 'react';
import Layout from './layout/Layout';

const Home = () => {
    return (
        <Layout>
            <div>You are on the Homepage.</div>
        </Layout>
    )
};

export default Home;